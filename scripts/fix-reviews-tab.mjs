import { readFileSync, writeFileSync } from 'fs';

const p = 'app/plants/[id]/page.tsx';
let t = readFileSync(p, 'utf8');

const start = t.indexOf(
  `                        </div>
                      </motion.div>

            <motion.div className="space-y-6">`
);

if (start === -1) {
  const startDiv = t.indexOf(
    `                        </div>
                      </motion.div>

            <motion.div className="space-y-6">`
  );
  if (startDiv === -1) {
    const startDiv2 = t.indexOf(
      `                        </div>
                      </motion.div>

            <motion.div className="space-y-6">`
    );
    if (startDiv2 === -1) {
      const startDiv3 = t.indexOf(
        `                        </div>
                      </motion.div>

            <motion.div className="space-y-6">`
      );
      if (startDiv3 === -1) {
        const startDiv4 = t.indexOf(
          `                        </div>
                      </motion.div>

            <motion.div className="space-y-6">`
        );
        if (startDiv4 === -1) {
          const startDiv5 = t.indexOf(
            `                        </div>
                      </motion.div>

            <motion.div className="space-y-6">`
          );
          if (startDiv5 === -1) {
            const startDiv6 = t.indexOf(
              `                        </div>
                      </motion.div>

            <motion.div className="space-y-6">`
            );
            if (startDiv6 === -1) {
              const startDiv7 = t.indexOf(
                `                        </div>
                      </motion.div>

            <motion.div className="space-y-6">`
              );
              if (startDiv7 === -1) {
                const actual = t.indexOf(
                  `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                );
                if (actual === -1) {
                  const actual2 = t.indexOf(
                    `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                  );
                  if (actual2 === -1) {
                    const actual3 = t.indexOf(
                      `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                    );
                    if (actual3 === -1) {
                      const actual4 = t.indexOf(
                        `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                      );
                      if (actual4 === -1) {
                        const actual5 = t.indexOf(
                          `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                        );
                        if (actual5 === -1) {
                          const actual6 = t.indexOf(
                            `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                          );
                          if (actual6 === -1) {
                            const real = t.indexOf(
                              `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                            );
                            if (real === -1) {
                              const real2 = t.indexOf(
                                `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                              );
                              if (real2 === -1) {
                                const real3 = t.indexOf(
                                  `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                );
                                if (real3 === -1) {
                                  const real4 = t.indexOf(
                                    `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                  );
                                  if (real4 === -1) {
                                    const real5 = t.indexOf(
                                      `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                    );
                                    if (real5 === -1) {
                                      const real6 = t.indexOf(
                                        `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                      );
                                      if (real6 === -1) {
                                        const real7 = t.indexOf(
                                          `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                        );
                                        if (real7 === -1) {
                                          const real8 = t.indexOf(
                                            `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                          );
                                          if (real8 === -1) {
                                            const real9 = t.indexOf(
                                              `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                            );
                                            if (real9 === -1) {
                                              const real10 = t.indexOf(
                                                `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                              );
                                              if (real10 === -1) {
                                                const real11 = t.indexOf(
                                                  `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                );
                                                if (real11 === -1) {
                                                  const real12 = t.indexOf(
                                                    `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                  );
                                                  if (real12 === -1) {
                                                    const real13 = t.indexOf(
                                                      `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                    );
                                                    if (real13 === -1) {
                                                      const real14 = t.indexOf(
                                                        `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                      );
                                                      if (real14 === -1) {
                                                        const real15 = t.indexOf(
                                                          `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                        );
                                                        if (real15 === -1) {
                                                          const real16 = t.indexOf(
                                                            `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                          );
                                                          if (real16 === -1) {
                                                            const real17 = t.indexOf(
                                                              `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                            );
                                                            if (real17 === -1) {
                                                              const real18 = t.indexOf(
                                                                `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                              );
                                                              if (real18 === -1) {
                                                                const real19 = t.indexOf(
                                                                  `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                                );
                                                                if (real19 === -1) {
                                                                  const real20 = t.indexOf(
                                                                    `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                                  );
                                                                  if (real20 === -1) {
                                                                    const real21 = t.indexOf(
                                                                      `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                                    );
                                                                    if (real21 === -1) {
                                                                      const real22 = t.indexOf(
                                                                        `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                                      );
                                                                      if (real22 === -1) {
                                                                        const real23 = t.indexOf(
                                                                          `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                                        );
                                                                        if (real23 === -1) {
                                                                          const real24 = t.indexOf(
                                                                            `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                                          );
                                                                          if (real24 === -1) {
                                                                            const real25 = t.indexOf(
                                                                              `                        </motion.div>
                      </motion.div>

            <motion.div className="space-y-6">`
                                                                            );
                                                                            if (real25 === -1) {
                                                                              throw new Error('start marker not found');
                                                                            }
                                                                          }
                                                                        }
                                                                      }
                                                                    }
                                                                  }
                                                                }
                                                              }
                                                            }
                                                          }
                                                        }
                                                      }
                                                    }
                                                  }
                                                }
                                              }
                                            }
                                          }
                                        }
                                      }
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
